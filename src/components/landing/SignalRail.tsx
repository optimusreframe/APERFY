import { Check, PackageCheck, ShoppingBag, Truck } from 'lucide-react';

export default function SignalRail({ locale }: { locale: 'en' | 'es' }) {
  const stages = locale === 'es'
    ? [{ label: 'Disponible', desc: 'El producto aparece en el catálogo.', icon: ShoppingBag }, { label: 'Revisado', desc: 'Los detalles importantes están visibles.', icon: Check }, { label: 'Añadido', desc: 'Puedes guardarlo en tu carrito.', icon: PackageCheck }, { label: 'Confirmado', desc: 'La orden se coordina por WhatsApp.', icon: Truck }]
    : [{ label: 'Available', desc: 'The product is live in the catalog.', icon: ShoppingBag }, { label: 'Reviewed', desc: 'The details that matter are visible.', icon: Check }, { label: 'Added', desc: 'You can save it to your cart.', icon: PackageCheck }, { label: 'Confirmed', desc: 'The order is coordinated on WhatsApp.', icon: Truck }];
  return <ol aria-label={locale === 'es' ? 'Cómo comprar en APERFY' : 'How to shop APERFY'} className="grid gap-5 md:grid-cols-4">{stages.map(({ label, desc, icon: Icon }, index) => <li key={label} className="relative flex gap-3"><div className="relative flex h-10 w-10 shrink-0 items-center justify-center rounded-full border border-primary/30 bg-primary/10 text-primary"><Icon className="h-4 w-4" aria-hidden="true" />{index < stages.length - 1 && <span className="absolute left-[calc(100%+0.75rem)] top-1/2 hidden h-px w-[calc(100%+1rem)] bg-primary/20 md:block" />}</div><div><h3 className="font-semibold tracking-tight text-foreground">{label}</h3><p className="mt-1 text-sm leading-relaxed text-muted-foreground">{desc}</p></div></li>)}</ol>;
}
